import React from 'react';
import { Typography } from '@mui/material';

const UploadNewProblem = () => {
	const tmp = [];
	return <Typography variant="h1">UploadNewProblem</Typography>;
};

export default UploadNewProblem;
